<div class="home">
  <p>Count your money :)</p>

  <% unless current_user %>
    <p>But first <%= link_to 'register', new_user_path %> or <%= link_to 'sign in', login_path%> (:</p>
  <% end %>

  <% if current_user %>
    <table>
      <tr>
        <td>Your total earnings</td>
        <td class=""><%= number_with_delimiter(@total_earnings) %></td>
      </tr>

      <tr>
        <td>Your total spendings</td>
        <td><%= number_with_delimiter(@total_spendings) %></td>
      </tr>

      <tr>
        <td>Balance:</td>
        <td class='<%= @balance > 0 ? 'green' : 'red' %>'><%= number_with_delimiter(@balance) %></td>
      </tr>
    </table>

    <div class="earning">
      <%= link_to 'Add Earning', new_user_earning_path(current_user) %>
    </div>

    <div class="spending">
      <%= link_to 'Add Spending', new_user_spending_path(current_user) %>
    </div>
  <% end %>


</div>